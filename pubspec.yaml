name: school_notifications
description: A new Flutter project

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🎨 UI
  cupertino_icons: ^1.0.8

  # 🔥 Firebase (للإشعارات لاحقًا)
  firebase_core: ^3.15.2
  firebase_messaging: ^15.2.10

  # 🗄️ Supabase (الربط مع منصتك الحالية)
  supabase_flutter: ^2.5.0
  # إشعارات محلية + نغمة
  flutter_local_notifications: ^13.0.0
  flutter_ringtone_player: ^3.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true